#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef long double ld;
typedef vector<int> vi;
typedef vector<ll> vll;
typedef pair<int,int> pii;
typedef pair<ll,ll> pll;
#define pb push_back
#define Sort(a) sort(a.begin(),a.end())
#define fo(i,n) for(long long i=0;i<n;i++)
#define read(a,n) for(long long i=0;i<n;i++){cin>>a[i];}
const int N=200001;

int main(){
    int n,m;
    cin>>n>>m;
    vi adj[N];
    for(int i=0;i<m;i++){
        int x,y;
        cin>>x>>y;
        adj[x].pb(y);
        adj[y].pb(x);    }
        
    bool visited[N];
    int distance[N];
    queue<int>q;
    vi p(N);
    distance[1]=0;
    visited[1]=true;
    q.push(1);
    while(!(q.empty())){
        int s=q.front();
        q.pop();
    
        for(int x:adj[s]){
            if(visited[x])continue;
            distance[x]=distance[s]+1;
            visited[x]=true;
            q.push(x);
            p[x]=s;
            }}
     vi ans;
     if(!visited[n]){ cout<<"IMPOSSIBLE"<<endl;return 0;}
     
    for(int i=n;i>1;i=p[i]){
        ans.pb(i);
    }
    ans.pb(1);
    
    cout<<ans.size()<<endl;
    for(int i=ans.size()-1;i>=0;i--){
        cout<<ans[i]<<" ";}
        cout<<endl;
    }