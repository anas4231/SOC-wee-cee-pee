#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	// your code goes here
	int n,x;
	ll m=1e9+7;
	cin>>n>>x;
	 int c[n];
	 for(int i=0;i<n;i++){
	     cin>>c[i];
	 }
	  vector<ll>dp(x+1,0);
dp[0]=1;
	  for(int i=0;i<n;i++){
	      for(int j=c[i];j<=x;j++){
	        dp[j]=(dp[j]+dp[j-c[i]])%m;
	      }}
	      cout<<dp[x]<<endl;
	      
	  }
	  